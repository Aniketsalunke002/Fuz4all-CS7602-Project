Fix the following C++ program so it compiles with g++ -std=c++23. Preserve the overall structure: keep templates, classes, operator overloading, and STL/concepts usage. Only fix what is necessary for compilation.

Program:
{program}

Compiler stderr:
{stderr}

Output ONLY the fixed C++ code. No explanations, no markdown. Keep interesting constructs. Add missing includes if needed. The code must compile with -std=c++23 and must not require external files.
