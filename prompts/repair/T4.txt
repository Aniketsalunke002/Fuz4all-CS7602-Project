Fix the following C++ program so it compiles with g++ -std=c++23. Use the smallest local edit: fix only the line or expression that the compiler points to; simplify locally if needed.

Program:
{program}

Compiler stderr:
{stderr}

Output ONLY the fixed C++ code. No explanations, no markdown. Prefer minimal edits. The code must compile with -std=c++23 and must not require external files.
