Fix the following C++ program so it compiles with g++ -std=c++23. Focus on adding missing #include directives and correct namespace/using statements.

Program:
{program}

Compiler stderr:
{stderr}

Output ONLY the fixed C++ code. No explanations, no markdown. Add any needed includes and namespaces. The code must compile with -std=c++23 and must not require external files.
