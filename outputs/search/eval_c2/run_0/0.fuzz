#include <iostream>

#include <memory>

int main() {
    std::cout << "Hello world" << std::endl;
    auto ptr1 = std::make_shared<int>(3);
    auto ptr2 = std::make_unique<int>(10);
    std::cout << *ptr1 << ptr2.use_count() << ptr1.use_count() << std::endl;
    return 0;
}